func SanitizeString(string text) {
	StringBuilder newText = new StringBuilder()
	for int i = 0; i < text.length(); i++ {
    if text[i] == "/" {
      break
    } else if text[i] != "$" {
      newText.write(text[i])
		}
	}
	return newText.string()
}
