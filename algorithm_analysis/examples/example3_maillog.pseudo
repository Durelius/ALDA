int maxSize = 500
func StripMailList(List mailList) {
	while mailList.size() > maxSize {
    int mailSize = mailList.size()
		mailList = mailList.subList(mailSize / 2, mailSize)
	}
	return mailList
}

